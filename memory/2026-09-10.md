# 2026-09-10 — pausa pro Mint (Stylus continua vivo)

## O que aconteceu hoje
- Vita: BTD6 era furada (APK arm64, Vita é ARMv7 32-bit, sem port) — stub
  `app/BTD6GAME1` + `data/btd6` + `btd6_port.vpk` APAGADOS do SD. BTD5
  (`app/BTD500001` + `data/btd5`) intacto, não tocar.
- Vita/Balatro: `game.love` (46MB, zip válido, main.lua ok) gerado via xdelta
  do Balatro.exe do Steam + patch oficial v0.13.1, copiado pra
  `ux0:app/BALA00001/game.love`. Era o que faltava pro jogo abrir.
  Arquivos errados que eu tinha posto em `ux0:data/balatro/` foram removidos.
- Vita/Moonlight: `moonlight.vpk` 0.13.2 na raiz do SD, instalar pelo VitaShell.
- PC: Sunshine (flatpak) rodando + autostart (user unit) + ufw liberado +
  tampa-ignora-suspend (drop-in igual ao da fonte + sleep targets mascarados).
  **Tudo isso MORRE com o wipe — refazer no Mint.**
- Limpeza PC: ~3GB (cache pacman/yay/uv/pip + meu scratch). Músicas intactas.
- Repos: `stylus` main e241140 e `vitastylus` (branch vitastylus-standalone
  e20816d) com push feito, árvores limpas. Músicas todas no celular.
- USB Cruzer Blade 14.9G (/dev/sda) virou VENTOY com os dois ISOs:
  `linuxmint-22.3-cinnamon-64bit.iso` (sha256 ok) + `stylus_202608.iso`
  (imagem dd da partição, ISO9660 genuíno verificado). Boot menu na hora de ligar.

## Decisões (não reabrir sem motivo)
- Isaac Repentance NUNCA saiu pra Vita (só Rebirth PCSE-00507). Só via
  Moonlight (stream do PC). Não existe port, não adianta procurar.
- Cryptid/mods com Lovely no Balatro Vita: não. Lovely é nativo só de
  desktop, Cryptid tem 15+ patches + 24MB, Vita faz ~20fps no base.
  Mods Lua puros pequenos, talvez — pedir a lista dele.
- Stylus NÃO foi abandonado, só pausa de vibe (ele quer Win7 no Mint).

## Pendente pra próxima sessão (máquina Mint)
1. Recriar host Sunshine no Mint (flatpak + ufw + tampa).
2. Portar prateleira (Super+M → stylus-shelf) + ferramentas de música + UI
   pygame pro Mint; atalhos como Custom Shortcuts do Cinnamon.
3. Ele testa Balatro e Moonlight no Vita e reporta.
