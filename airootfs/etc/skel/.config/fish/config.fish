# STYLUS — fish configuration
#
# Most of the setup lives in conf.d/stylus.fish (colours, abbreviations, aliases,
# environment) and functions/ (prompt, `stylus` command, helpers). This file is
# yours to change.

# The greeting itself lives in functions/fish_greeting.fish: full system
# information in the first shell of a session, one line in the rest.
