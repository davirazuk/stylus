# ─────────────────────────────────────────────────────────────────────────────
#  STYLUS — GRUB theme
#
#  This is the screen a dual-booting student sees every time the machine starts,
#  so it should not be stock blue-on-black. Colours only, no nine-slice pixmaps:
#  a misaligned slice looks far worse than none, and the built-in Unifont is
#  always present, so nothing here depends on grub-mkfont having run.
# ─────────────────────────────────────────────────────────────────────────────

desktop-image: "background.png"
desktop-color: "#07080b"
title-text: ""
message-color: "#e2e7f0"
message-bg-color: "#07080b"
terminal-font: "Unifont Regular 16"
terminal-left: "8%"
terminal-top: "20%"
terminal-width: "84%"
terminal-height: "60%"
terminal-border: "0"

# ── The menu ─────────────────────────────────────────────────────────────────
+ boot_menu {
    left               = 22%
    top                = 34%
    width              = 56%
    height             = 44%

    item_font          = "Unifont Regular 16"
    item_color         = "#b9d4c6"
    item_height        = 34
    item_padding       = 6
    item_spacing       = 8
    item_icon_space    = 0
    icon_width         = 0
    icon_height        = 0

    selected_item_font  = "Unifont Regular 16"
    selected_item_color = "#5bcefa"

    scrollbar          = true
    scrollbar_width    = 4
    scrollbar_thumb_color = "#5bcefa"
    scrollbar_frame_color = "#0d0f14"
}

# ── Countdown ────────────────────────────────────────────────────────────────
+ progress_bar {
    id        = "__timeout__"
    left      = 30%
    top       = 82%
    width     = 40%
    height    = 12
    show_text = true
    text      = "Iniciando em %d s"
    text_color = "#7e899c"
    font      = "Unifont Regular 16"
    bar_style = "none"
    fg_color  = "#5bcefa"
    bg_color  = "#0d0f14"
    border_color = "#0d0f14"
}

# ── Hints ────────────────────────────────────────────────────────────────────
+ label {
    left  = 0
    top   = 90%
    width = 100%
    align = "center"
    color = "#7e899c"
    font  = "Unifont Regular 16"
    text  = "↑↓ escolher     Enter iniciar     E editar     C linha de comando"
}
